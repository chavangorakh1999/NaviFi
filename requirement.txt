# Core dependencies (actually used in the codebase)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
requests>=2.31.0
aiohttp>=3.9.0

# Testing dependencies (used in test files)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Optional dependencies (for full functionality if available)
# Note: These may not be available in public PyPI
# google-adk>=0.1.0
# google-generativeai>=0.3.0
# google-cloud-aiplatform>=1.35.0
# mcp

# Security dependencies (optional - for future authentication features)
# python-multipart>=0.0.6
# python-jose[cryptography]>=3.3.0
# passlib[bcrypt]>=1.7.4

# Note: The simplified version works with just the core dependencies above
# If you have access to Google ADK, uncomment the optional dependencies